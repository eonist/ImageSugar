// swift-tools-version:5.3
import PackageDescription

let package = Package(
    name: "ImageSugar",
    products: [
        .library(
            name: "ImageSugar",
            targets: ["ImageSugar"])
    ],
    dependencies: [],
    targets: [
        .target(
            name: "ImageSugar",
            dependencies: [])
    ]
)
